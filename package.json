{
  "name": "spon-spuiterij-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:v1": "npm run dev -w v1",
    "dev:v2": "npm run dev -w v2",
    "build": "npm run build -w v1 && npm run build -w v2 && node scripts/assemble-landing.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
